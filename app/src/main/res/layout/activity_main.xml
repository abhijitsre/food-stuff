<?xml version="1.0" encoding="utf-8"?>

<!-- main layout that holds the recycler view (displaying a list of food stuffs) and the floating action button to add new item-->

<FrameLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/recyclerViewFoodStuffs"
        />

    <!-- Maybe need to add padding for recyclerview-->

    <android.support.design.widget.FloatingActionButton
        android:backgroundTint="@android:color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/fab_addnew"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        app:srcCompat="@android:drawable/ic_input_add"
        android:tint="@android:color/white"/>


</FrameLayout>
